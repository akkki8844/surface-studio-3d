/* ============================================================
   SHADERS — reserved for future custom effects.
   The surface now uses vertex-colored MeshPhongMaterial
   which works reliably across all browsers / WebGL versions.
   ============================================================ */
(function (FT3D) {
  "use strict";
  FT3D.shaders = {};
  FT3D.log.info("shaders.js loaded (standard materials active)");
})(window.FT3D);
